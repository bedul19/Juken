<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ios_background">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView style="@style/IosLargeTitle" android:text="Connect" android:paddingBottom="4dp" />
        <TextView style="@style/IosCaption" android:text="Hubungkan ke ECU via Bluetooth" android:paddingBottom="16dp" />

        <!-- Status card -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/bg_ios_card"
            android:padding="16dp"
            android:gravity="center_vertical"
            android:layout_marginBottom="20dp">

            <View
                android:id="@+id/statusDot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:background="@drawable/dot_gray" />

            <TextView
                android:id="@+id/statusText"
                style="@style/IosRowTitle"
                android:layout_marginStart="10dp"
                android:text="Belum terhubung" />
        </LinearLayout>

        <TextView style="@style/IosSectionHeader" android:text="Perangkat Terpasang" android:paddingBottom="6dp" />

        <LinearLayout
            android:id="@+id/deviceListContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_ios_card"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/refreshButton"
            style="@style/IosButtonPrimary"
            android:text="Refresh Daftar Perangkat"
            android:layout_marginBottom="10dp" />

        <Button
            android:id="@+id/disconnectButton"
            style="@style/IosButtonDestructive"
            android:text="Putuskan Koneksi" />

    </LinearLayout>
</ScrollView>
